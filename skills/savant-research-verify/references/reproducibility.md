# Reproducibility mode

Record:

- exact inputs and source paths;
- commands and tool versions;
- random seeds and environment assumptions;
- generated artifacts and their existence;
- whether a checker derives state from the source or validates freshness;
- unresolved external dependencies.

An exit code alone is not evidence that an expected artifact exists.

